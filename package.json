{
  "name": "office-management-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install": "cd angular-app && npm install",
    "build": "cd angular-app && npm run build -- --configuration production"
  }
}
